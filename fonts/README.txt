SOMEWHERE brand fonts — Haas Grot Disp R Trial

Add your font files to this folder so they load on the live site:

  HaasGrotDispRTrial-45Light.woff2   (body, paragraphs, nav — 45 Light)
  HaasGrotDispRTrial-55Roman.woff2   (headings, buttons, logo SOME — 55 Roman)

.otf or .woff also work if you update paths in styles.css.

Logo split:
  SOME  → 55 Roman
  WHERE → 45 Light

Site typography:
  Body & UI copy → 45 Light (font-weight 300–400)
  Headings & emphasis → 55 Roman (font-weight 500–700)

If your files use different names (e.g. from Lineto zip), rename them to match
or tell us the exact filenames.
